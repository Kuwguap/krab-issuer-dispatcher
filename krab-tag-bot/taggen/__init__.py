# The canonical generator (tag_pdf.py) is copied here from
# ../krableadsV2/utils/tag_pdf.py at build time (see render.yaml buildCommand),
# with the templates + fonts copied to ../assets/. Kept out of git so there is
# a single source of truth; tagcore.py falls back to the krableadsV2 copy for
# local dev and tests.
