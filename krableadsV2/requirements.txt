python-telegram-bot[job-queue]>=20.0
supabase>=2.28.0
python-dotenv>=1.0.0
requests>=2.31.0
pytz>=2025.1
flask>=3.0.0
flask-cors>=4.0.0
openai>=1.0.0
pymupdf>=1.24.0
# NY FS-20 insurance-card issuance (PDF + AAMVA PDF417 barcodes + Resend email)
reportlab>=4.0.0
pdf417gen>=0.7.1
pillow>=10.0.0
# OCR-based phone-number localization for image redaction. Requires the
# tesseract system binary (apt-get install tesseract-ocr).
pytesseract>=0.3.10
resend>=2.0.0
# Error reporting. Optional at runtime: utils/observability.py is a no-op
# without SENTRY_DSN, so a developer machine reports nothing anywhere.
sentry-sdk>=2.0.0
