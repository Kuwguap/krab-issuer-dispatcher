# Field example images

Replace any file here with a real screenshot (keep the same filename, or update `static/main.js`).

| File | Used next to |
|------|----------------|
| `example-drivers-license.svg` | License photo upload |
| `example-mailing-address.svg` | Mailing address |
| `example-telegram-username.svg` | Telegram @username |
| `example-payment.svg` | Payment method |
| `example-full-name.svg` | Full name |

Tip: PNG/JPG works too — e.g. drop `example-drivers-license.jpg` and change the path in `apply.html` + `main.js`.
