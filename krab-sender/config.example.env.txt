TELEGRAM_BOT_TOKEN=YOUR_TELEGRAM_BOT_TOKEN_HERE

# Email delivery configuration
# For Gmail SMTP, set:
#   EMAIL_PROVIDER=gmail_smtp
#   EMAIL_SMTP_USERNAME=your_gmail_address@gmail.com
#   EMAIL_SMTP_PASSWORD=your_app_password_here
# NOTE: Use a Google App Password, NOT your normal Gmail password.
EMAIL_PROVIDER=stub
EMAIL_FROM_ADDRESS=krab-dispatch@example.com
EMAIL_TO_ADDRESS=destination@example.com
EMAIL_SMTP_HOST=smtp.gmail.com
EMAIL_SMTP_PORT=587
EMAIL_SMTP_USERNAME=
EMAIL_SMTP_PASSWORD=

# Database configuration
# For Supabase/Postgres, set DATABASE_URL to the full connection string, e.g.:
#   DATABASE_URL=postgresql://postgres:YOUR_PASSWORD@YOUR_HOST:5432/postgres
DATABASE_URL=

# Optional: same Supabase project as Krab Issuer (service role key, server-side only).
# Enables: auto-match PDF filename → open lead/reference, mark tag sent, show receipt in Krab Dispatch admin.
# Run Krab Issuer repo folder `krableadsV2/database/migration_krab_dispatch_tag_tracking.sql` in Supabase first.
# SUPABASE_URL=https://YOUR_PROJECT.supabase.co
# SUPABASE_SERVICE_ROLE_KEY=

# Admin dashboard password (MUST be overridden in real environments)
ADMIN_PASSWORD=AdminPassword123!

# API CORS (web service only). Comma-separated origins, or override regex.
# Extra origins in CORS_ORIGINS are merged with the built-in Vercel + localhost
# list so production dashboards stay allowed.
# Example:
#   CORS_ORIGINS=https://krabdispatch.vercel.app,https://krab-dispatch.vercel.app
#   CORS_ORIGIN_REGEX=^https://.*\.vercel\.app$
CORS_ORIGINS=
CORS_ORIGIN_REGEX=

# API base URL (for bot to fetch recipients and transactions)
# For local dev: http://127.0.0.1:8000
# For production: https://krab-dispatch-api.onrender.com
API_BASE_URL=http://127.0.0.1:8000

# Optional: enable admin AI data Q&A via OpenAI.
# Keep secret server-side (Render environment variable), never in frontend code.
OPENAI_API_KEY=

# Optional: Telegram chat ID (group or channel) to notify when a send is successful.
# Add the bot to the group, then get the chat ID (e.g. -1001234567890) and set it here.
# Leave empty to disable group notifications.
# ISSUER_GROUP_CHAT_ID=

# Optional: classify senders for dashboard "issuer group" metrics.
# Priority 1: if chat ID matches HIGHKAGE_TEAM_CHAT_ID => highkage_group.
# Otherwise issuer group defaults to sensei_group.
HIGHKAGE_TEAM_CHAT_ID=
#
# Optional fallback classification by handles (used if chat ID is unavailable).
# Comma-separated Telegram @handles (without spaces required).
# Example: SENSEI_GROUP_HANDLES=sensei_one,sensei_two
# If HIGHKAGE_GROUP_HANDLES is empty, backend defaults to: haruhatsu
SENSEI_GROUP_HANDLES=
HIGHKAGE_GROUP_HANDLES=

# Insurance card flow (Resend + TriStateCoverage portal) — same stack as krableadsV2
RESEND_API_KEY=
RESEND_FROM=Tri State Coverage <hello@yourdomain.com>
INTEGRATIONS_API_KEY=
TRISTATECOVERAGE_API_BASE=https://tristatecoverage.com
INSURANCE_ISSUER_NAME=Serviced by AIPSO-SAIP
INSURANCE_ISSUER_PHONE=
INSURANCE_ISSUER_ADDRESS=PO Box 6400|Providence, RI 02940-6200
INSURANCE_CARRIER_NAME=169 National Specialty Insurance Company

# NJ Temporary Evidence of Insurance (upstream barcode-app HTTP endpoint)
BARCODE_APP_BASE_URL=
